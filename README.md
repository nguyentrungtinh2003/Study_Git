# Study Git basic

Git Commands

```bash
git clone, git init, git add, git push, git pull, git merge, git branch, git checkout, git checkout -b, git status, git log
```

# Practice Git Commands

Try create many branch main, dev, feature/add-navbar, feature/footer etc. to practice add files and commit, create pull request many branch

# Practice Write README.md

Try write many content for README.md
